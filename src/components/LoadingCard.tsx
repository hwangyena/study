import Skeleton from '@mui/material/Skeleton';

const LoadingCard = () => {
  // You can add any UI inside Loading, including a Skeleton.
  return <Skeleton variant='rectangular' width={275} height={90} />;
};

export default LoadingCard;
